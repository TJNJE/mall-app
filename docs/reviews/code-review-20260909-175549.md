# Code Review — Phase 3 S2 提交规范

## Scope

- Mode: current changes
- Branch: `feat/eng-gaps-implement`
- Base: `50f08fb`（S1 accepted slice commit）
- Output file: `docs/reviews/code-review-20260909-175549.md`
- Included scope: 新增 `commitlint.config.cjs`、`.husky/commit-msg`；`package.json`（devDeps @commitlint/cli + config-conventional）
- Excluded scope: package-lock
- Parallel review coverage: 无（两文件小改动）

## Findings

### F1-未修复-低-`gateflow:` 前缀不在 type-enum 内，后续流程 commit 需改用合法 type

- **入口/函数**: config-conventional 的 `type-enum` 规则
- **文件(行号)**: `commitlint.config.cjs`（未覆写 type-enum）
- **输入场景**: 后续 gateflow 的 accept/closeout commit 若沿用 `gateflow:` 前缀
- **实际分支**: commit-msg hook 将拒绝该类 message
- **预期行为**: hook 生效后所有新 commit 应符合 Conventional Commits
- **实际行为**: 历史上使用过的 `gateflow:` type 不在允许列表（build/chore/ci/docs/feat/fix/perf/refactor/revert/style/test）
- **直接证据**: 本仓库历史含 `gateflow: accept plan...` 等提交；commitlint 反例验证输出 "found 2 problems"
- **影响**: 主控制器后续创建 commit 时若用旧前缀会被 hook 拦截（预期行为，非缺陷，但需知晓）
- **建议改法和验证点**: 后续 commit 改用 `chore(eng):` / `docs(eng):` 等合法 type；不将 `gateflow` 加入白名单（保持标准）
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低
- **裁决**: accepted-as-is（流程约定调整，非代码问题）

### F2-未修复-低-历史 commit 不符合新规范（预期内豁免）

- **入口/函数**: 无（存量）
- **文件(行号)**: git 历史（含 `gateflow:`、部分中文 subject 等不符合 conventional 的提交）
- **输入场景**: `npx commitlint --from <old>` 校验历史
- **实际分支**: 历史提交不满足新规则
- **预期行为**: 规范仅约束新增提交（plan 已声明历史豁免）
- **实际行为**: 历史提交保持原样
- **直接证据**: plan S2 non-goals「历史豁免：仅校验新增提交」
- **影响**: 无（设计决定）；如需重写历史需 rebase，风险大于收益
- **建议改法和验证点**: 无需动作；commitlint 从 hook 生效点向前校验
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低
- **裁决**: deferred-with-owner（显式豁免，永久）

## 关键验证记录

| 验证项        | 结果 | 证据                                                                                                                 |
| ------------- | ---- | -------------------------------------------------------------------------------------------------------------------- |
| 反例拒绝      | ✅   | `echo "bad message" \| npx commitlint` 输出 "found 2 problems"                                                       |
| 合规通过      | ✅   | `echo "feat(eng): 测试提交" \| npx commitlint` 退出 0                                                                |
| hook 生效机制 | ✅   | husky 9：`core.hooksPath=.husky/_`（包装器目录），用户文件无需执行位（与 pre-commit 同机制，此前多次 commit 已验证） |
| scope 白名单  | ✅   | scope-enum 含 eng/auth/product/cart/order/lib/ui/api/mock/docs                                                       |

## Open Questions

无。

## Residual Risk

- `semantic-release` 未引入（plan 显式 non-goal，无发布场景），CHANGELOG 缺位
- commit-msg hook 仅校验 message，不校验内容质量（预期）
