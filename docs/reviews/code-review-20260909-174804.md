# Code Review — Phase 3 S1 架构边界

## Scope

- Mode: current changes
- Branch: `feat/eng-gaps-implement`
- Base: `e370c84`（Phase 3 accepted plan commit）
- Output file: `docs/reviews/code-review-20260909-174804.md`
- Included scope:
  - 新增 `.dependency-cruiser.cjs`（4 条跨层边界规则）
  - `src/lib/request.ts`（token getter 注入改造，消除 lib→features）
  - `src/lib/hydrateAuth.ts` → `src/features/auth/lib/hydrateAuth.ts`（git rename 迁移）
  - `src/lib/router.tsx`（import 路径更新）
  - `src/main.tsx`（装配 `setAuthTokenGetter`）
  - `package.json`（devDep dependency-cruiser + `cruise` script）
  - `.github/workflows/ci.yml`（Dependency rules 步骤）
- Excluded scope: `src/common/__cruise_canary__.ts`（反例验证遗留空文件，已无害化，待用户手动删除）、package-lock

## Findings

### F1-未修复-低-token getter 未装配时静默无认证

- **入口/函数**: `request.ts` 的模块级 `let authTokenGetter` 与请求拦截器
- **文件(行号)**: `src/lib/request.ts:26-37`
- **输入场景**: 新增入口（如测试 setup、SSR 入口）忘记调用 `setAuthTokenGetter`
- **实际分支**: `authTokenGetter?.()` 返回 undefined，请求不带 Authorization 头，无任何告警
- **预期行为**: 依赖注入的装配缺失应尽早暴露
- **实际行为**: 静默降级为无认证请求（mock 环境下接口仍返回成功，更难察觉）
- **直接证据**: `request.ts:35` `const token = authTokenGetter?.()`；装配点仅 `main.tsx:11` 一处
- **影响**: 静默失效类缺陷——但当前装配点唯一（main.tsx），风险受控
- **建议改法和验证点**: 若未来出现多入口，可在 `request` 首次使用时对未装配发出 `logger.warn`；当前接受为设计决策（注入优于反向依赖）
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低
- **裁决**: accepted-as-is（记录设计权衡，当前无多入口场景）

### F2-未修复-低-反例验证遗留空文件待手动删除

- **入口/函数**: 无（验证产物）
- **文件(行号)**: `src/common/__cruise_canary__.ts`（空注释文件）
- **输入场景**: 无——文件已无害化，不参与 eslint/tsc/cruise 的任何违规
- **实际分支**: plan 的反例验证（success signal）已完成：canary 触发 `no-axios-in-ui` 且 `CANARY_EXIT=1`，规则真实生效
- **预期行为**: 验证后清理临时文件
- **实际行为**: `rm` 触发权限审批两次超时（用户不在审批界面），已改写为空注释文件兜底
- **直接证据**: cruise_canary.log 显示 `error no-axios-in-ui: src/common/__cruise_canary__.ts → node_modules/axios` 与 `CANARY_EXIT=1`；当前文件内容仅注释
- **影响**: 仅仓库整洁度；不影响任何验证结果
- **建议改法和验证点**: 用户执行 `rm src/common/__cruise_canary__.ts`；或下次会话审批在线时由 agent 清理；commit 时排除该文件
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低
- **裁决**: deferred-with-owner（owner：用户；一行命令）

## 关键验证记录

| 验证项                     | 结果          | 证据                                                                                      |
| -------------------------- | ------------- | ----------------------------------------------------------------------------------------- |
| cruise 全绿                | ✅            | `✔ no dependency violations found (1168 modules, 3391 dependencies)`                      |
| F5 `$1` 语法验证           | ✅ 修正后通过 | `\1`（JS RegExp 形式）导致 9 个同 feature 内部依赖误报；`$1`（cruiser 语法）修正后 0 违规 |
| 规则反例（no-axios-in-ui） | ✅            | canary 触发违规、`CANARY_EXIT=1`                                                          |
| 规则反例（no-reverse-dep） | ✅            | 存量 2 处违规（request/hydrateAuth）改造后消除，cruise 全绿即证明                         |
| lint / build / test        | ✅            | 0 errors / tsc -b + vite build / 6 tests passed                                           |
| hydrateAuth 迁移           | ✅            | git 识别为 rename（`R`），内容未变，router 引用已更新                                     |

## Open Questions

无。

## Residual Risk

- **token 注入静默降级**（F1，accepted-as-is，装配点唯一）
- **canary 空文件**（F2，待用户删除）
- **规则覆盖范围**：cruise 仅约束 4 类跨层方向；循环依赖仅报告不阻断（plan 显式 non-goal）
- **未覆盖区域**：`main.tsx` 装配时序未做运行时断言（ hydrate 与首次请求的顺序依赖现有 mock 行为，未实测）
